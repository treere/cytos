uprobe:$1:trace_node_step {
  @node[comm] = arg0;
  @start[comm] = nsecs;
}

uretprobe:$1:trace_node_step /@start[comm]/ {
  $n = @node[comm];
  @ns[comm, $n] = hist(nsecs - @start[comm]);
  delete(@start[comm]);
  delete(@node[comm]);
}
